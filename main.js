function sumNumber(a, b) {
  return a + b;
}

const sumResult = sumNumber(2, 1);
console.log(sumResult);

function multiplyNumber(a, b) {
  return a * b;
}

const multiplyResult = multiplyNumber(3, 5);
console.log(multiplyResult);
